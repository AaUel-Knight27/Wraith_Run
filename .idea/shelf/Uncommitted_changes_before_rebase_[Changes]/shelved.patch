Index: scripts/player/Health.cs
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>using Godot;\n\n/// <summary>\n/// Networked health and death/respawn. Lives on the owning peer's Player instance.\n/// CurrentHealth is replicated outward by PlayerNetworkSynchronizer, so every peer's copy of\n/// this node ends up running the same setter - that's what actually hides the model and\n/// disables the hitbox on remote clients, not just the local owner.\n/// </summary>\npublic partial class Health : Node\n{\n    [Signal] public delegate void DiedEventHandler();\n    [Signal] public delegate void RespawnedEventHandler();\n\n    private const float RespawnDelaySeconds = 3.0f;\n    private static readonly Vector3 SpawnAreaExtents = new(15.0f, 0.0f, 15.0f);\n\n    [Export] public float MaxHealth { get; set; } = 100.0f;\n\n    // Only the owning peer's ReceiveDamage call ever writes this directly. Everyone else's\n    // copy receives it through replication and just reacts in the setter below.\n    [Export]\n    public float CurrentHealth\n    {\n        get => _currentHealth;\n        set\n        {\n            bool wasAlive = _currentHealth > 0.0f || !_hasInitialized;\n            _hasInitialized = true;\n            _currentHealth = Mathf.Clamp(value, 0.0f, MaxHealth);\n            ApplyVisualState(_currentHealth > 0.0f);\n            if (wasAlive && _currentHealth <= 0.0f) EmitSignal(SignalName.Died);\n        }\n    }\n\n    public bool IsDead => _currentHealth <= 0.0f;\n    public float RespawnTimeRemaining => _respawnTimer;\n\n    private float _currentHealth;\n    private bool _hasInitialized;\n    private float _respawnTimer;\n    private CharacterBody3D _player = null!;\n    private CollisionShape3D? _collision;\n    private Node3D? _visualRoot;\n\n    public override void _Ready()\n    {\n        _player = (CharacterBody3D)GetParent();\n        _collision = _player.GetNodeOrNull<CollisionShape3D>(\"CollisionShape3D\");\n        _visualRoot = _player.GetNodeOrNull<Node3D>(\"Head/Armature_Arms\");\n        CurrentHealth = MaxHealth;\n    }\n\n    public override void _PhysicsProcess(double delta)\n    {\n        if (!IsMultiplayerAuthority() || !IsDead) return;\n        _respawnTimer -= (float)delta;\n        if (_respawnTimer <= 0.0f) Respawn();\n    }\n\n    /// <summary>\n    /// Called by an attacker's client via RpcId targeted at this player's owning peer. Mirrors\n    /// the client-authoritative model PlayerMovement already uses for position: this only takes\n    /// effect on the instance that actually owns the node, never on a remote observer's copy.\n    /// </summary>\n    [Rpc(MultiplayerApi.RpcMode.AnyPeer, TransferMode = MultiplayerPeer.TransferModeEnum.Reliable)]\n    public void ReceiveDamage(float amount, long attackerId)\n    {\n        if (!IsMultiplayerAuthority() || IsDead || amount <= 0.0f) return;\n        CurrentHealth -= amount;\n        if (IsDead) _respawnTimer = RespawnDelaySeconds;\n    }\n\n    private void Respawn()\n    {\n        var rng = new RandomNumberGenerator();\n        _player.Position = new Vector3(\n            rng.RandfRange(-SpawnAreaExtents.X, SpawnAreaExtents.X), 0.1f,\n            rng.RandfRange(-SpawnAreaExtents.Z, SpawnAreaExtents.Z));\n        _player.Velocity = Vector3.Zero;\n        CurrentHealth = MaxHealth;\n        EmitSignal(SignalName.Respawned);\n    }\n\n    private void ApplyVisualState(bool alive)\n    {\n        // References may not be assigned yet if replication delivers a value before this\n        // node's own _Ready() runs; _Ready() re-applies the correct state once it does.\n        if (_collision == null || _visualRoot == null) return;\n        _collision.Disabled = !alive;\n        _visualRoot.Visible = alive;\n    }\n}\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/scripts/player/Health.cs b/scripts/player/Health.cs
--- a/scripts/player/Health.cs	(revision 774c917d83a8063256c70186039af4df904bff27)
+++ b/scripts/player/Health.cs	(date 1789544127506)
@@ -3,8 +3,8 @@
 /// <summary>
 /// Networked health and death/respawn. Lives on the owning peer's Player instance.
 /// CurrentHealth is replicated outward by PlayerNetworkSynchronizer, so every peer's copy of
-/// this node ends up running the same setter - that's what actually hides the model and
-/// disables the hitbox on remote clients, not just the local owner.
+/// this node ends up running the same setter - that's what actually disables the hitbox and
+/// plays the death animation on remote clients, not just the local owner.
 /// </summary>
 public partial class Health : Node
 {
@@ -40,13 +40,13 @@
     private float _respawnTimer;
     private CharacterBody3D _player = null!;
     private CollisionShape3D? _collision;
-    private Node3D? _visualRoot;
+    private PlayerAnimationController? _animation;
 
     public override void _Ready()
     {
         _player = (CharacterBody3D)GetParent();
         _collision = _player.GetNodeOrNull<CollisionShape3D>("CollisionShape3D");
-        _visualRoot = _player.GetNodeOrNull<Node3D>("Head/Armature_Arms");
+        _animation = _player.GetNodeOrNull<PlayerAnimationController>("AnimationController");
         CurrentHealth = MaxHealth;
     }
 
@@ -78,15 +78,17 @@
             rng.RandfRange(-SpawnAreaExtents.Z, SpawnAreaExtents.Z));
         _player.Velocity = Vector3.Zero;
         CurrentHealth = MaxHealth;
+        _animation?.ResetAfterRespawn();
         EmitSignal(SignalName.Respawned);
     }
 
     private void ApplyVisualState(bool alive)
     {
-        // References may not be assigned yet if replication delivers a value before this
-        // node's own _Ready() runs; _Ready() re-applies the correct state once it does.
-        if (_collision == null || _visualRoot == null) return;
+        // The body mesh always stays visible now - death is shown by actually playing a death
+        // animation (see PlayDeath below), not by hiding the model. Collision still turns off
+        // so a corpse doesn't block foot traffic during the respawn delay.
+        if (_collision == null) return;
         _collision.Disabled = !alive;
-        _visualRoot.Visible = alive;
+        if (!alive) _animation?.PlayDeath();
     }
 }
Index: scenes/Player.tscn
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>[gd_scene load_steps=11 format=3]\n\n[ext_resource type=\"Script\" path=\"res://scripts/player/PlayerMovement.cs\" id=\"1\"]\n[ext_resource type=\"Script\" path=\"res://scripts/player/PlayerAnimationController.cs\" id=\"2\"]\n[ext_resource type=\"PackedScene\" path=\"res://art/characters/Armature/Model/Armature_arms.fbx\" id=\"3\"]\n[ext_resource type=\"Script\" path=\"res://scripts/player/FirstPersonArms.cs\" id=\"4\"]\n[ext_resource type=\"Script\" path=\"res://scripts/player/WeaponAttachment.cs\" id=\"5\"]\n[ext_resource type=\"Script\" path=\"res://scripts/player/WeaponSwitcher.cs\" id=\"6\"]\n[ext_resource type=\"Script\" path=\"res://scripts/network/PlayerNetworkSynchronizer.cs\" id=\"7\"]\n[ext_resource type=\"Script\" path=\"res://scripts/player/Health.cs\" id=\"8\"]\n[ext_resource type=\"Script\" path=\"res://scripts/player/PlayerHud.cs\" id=\"9\"]\n\n[sub_resource type=\"CapsuleShape3D\" id=\"1\"]\nradius = 0.35\nheight = 1.8\n\n[sub_resource type=\"SphereShape3D\" id=\"2\"]\nradius = 0.35\n\n[node name=\"Player\" type=\"CharacterBody3D\"]\nscript = ExtResource(\"1\")\n\n[node name=\"CollisionShape3D\" type=\"CollisionShape3D\" parent=\".\"]\nposition = Vector3(0, 0.9, 0)\nshape = SubResource(\"1\")\n\n[node name=\"GroundProbe\" type=\"ShapeCast3D\" parent=\".\"]\nposition = Vector3(0, 0.35, 0)\ntarget_position = Vector3(0, -0.2, 0)\nshape = SubResource(\"2\")\n\n[node name=\"Health\" type=\"Node\" parent=\".\"]\nscript = ExtResource(\"8\")\n\n[node name=\"Head\" type=\"Node3D\" parent=\".\"]\nposition = Vector3(0, 1.6, 0)\n\n[node name=\"FirstPersonCamera\" type=\"Camera3D\" parent=\"Head\"]\nposition = Vector3(0, 0.06, -0.09)\n\n[node name=\"Armature_Arms\" parent=\"Head\" instance=ExtResource(\"3\")]\ntransform = Transform3D(-1, 0, 8.742278e-08, 0, 1, 0, -8.742278e-08, 0, -1, -0.011, -1.61, 0)\nscript = ExtResource(\"4\")\n\n[node name=\"WeaponAttachment\" type=\"Node3D\" parent=\"Head/Armature_Arms\"]\nscript = ExtResource(\"5\")\n\n[node name=\"AnimationPlayer\" type=\"AnimationPlayer\" parent=\".\"]\n\n[node name=\"AnimationTree\" type=\"AnimationTree\" parent=\".\"]\nanim_player = NodePath(\"../AnimationPlayer\")\n\n[node name=\"AnimationController\" type=\"Node\" parent=\".\"]\nscript = ExtResource(\"2\")\n\n[node name=\"MultiplayerSynchronizer\" type=\"MultiplayerSynchronizer\" parent=\".\"]\n\n[node name=\"NetworkSynchronizerSetup\" type=\"Node\" parent=\".\"]\nscript = ExtResource(\"7\")\n\n[node name=\"WeaponSwitcher\" type=\"Node\" parent=\".\"]\nscript = ExtResource(\"6\")\n\n[node name=\"Hud\" type=\"CanvasLayer\" parent=\".\"]\nscript = ExtResource(\"9\")\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/scenes/Player.tscn b/scenes/Player.tscn
--- a/scenes/Player.tscn	(revision 774c917d83a8063256c70186039af4df904bff27)
+++ b/scenes/Player.tscn	(date 1789544127536)
@@ -1,9 +1,8 @@
-[gd_scene load_steps=11 format=3]
+[gd_scene load_steps=10 format=3]
 
 [ext_resource type="Script" path="res://scripts/player/PlayerMovement.cs" id="1"]
 [ext_resource type="Script" path="res://scripts/player/PlayerAnimationController.cs" id="2"]
-[ext_resource type="PackedScene" path="res://art/characters/Armature/Model/Armature_arms.fbx" id="3"]
-[ext_resource type="Script" path="res://scripts/player/FirstPersonArms.cs" id="4"]
+[ext_resource type="PackedScene" path="res://art/characters/manny.glb" id="3"]
 [ext_resource type="Script" path="res://scripts/player/WeaponAttachment.cs" id="5"]
 [ext_resource type="Script" path="res://scripts/player/WeaponSwitcher.cs" id="6"]
 [ext_resource type="Script" path="res://scripts/network/PlayerNetworkSynchronizer.cs" id="7"]
@@ -32,19 +31,17 @@
 [node name="Health" type="Node" parent="."]
 script = ExtResource("8")
 
+[node name="Armature" parent="." instance=ExtResource("3")]
+
+[node name="WeaponAttachment" type="Node3D" parent="Armature"]
+script = ExtResource("5")
+
 [node name="Head" type="Node3D" parent="."]
 position = Vector3(0, 1.6, 0)
 
 [node name="FirstPersonCamera" type="Camera3D" parent="Head"]
 position = Vector3(0, 0.06, -0.09)
 
-[node name="Armature_Arms" parent="Head" instance=ExtResource("3")]
-transform = Transform3D(-1, 0, 8.742278e-08, 0, 1, 0, -8.742278e-08, 0, -1, -0.011, -1.61, 0)
-script = ExtResource("4")
-
-[node name="WeaponAttachment" type="Node3D" parent="Head/Armature_Arms"]
-script = ExtResource("5")
-
 [node name="AnimationPlayer" type="AnimationPlayer" parent="."]
 
 [node name="AnimationTree" type="AnimationTree" parent="."]
Index: scenes/PlayerMovementTest.tscn
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>[gd_scene format=3 uid=\"uid://b3p1edfkfgoig\"]\n\n[ext_resource type=\"Script\" uid=\"uid://pikamp6vhtto\" path=\"res://scripts/player/PlayerMovement.cs\" id=\"1_player\"]\n[ext_resource type=\"Script\" uid=\"uid://nqxihnrb77iw\" path=\"res://scripts/player/PlayerAnimationController.cs\" id=\"2_animation\"]\n[ext_resource type=\"PackedScene\" uid=\"uid://8ice2iun26gc\" path=\"res://art/characters/Armature/Model/Armature_arms.fbx\" id=\"3_arms\"]\n[ext_resource type=\"Script\" uid=\"uid://cqb0f2qba4y6w\" path=\"res://scripts/player/FirstPersonArms.cs\" id=\"4_arms\"]\n\n[sub_resource type=\"CapsuleShape3D\" id=\"CapsuleShape_player\"]\nradius = 0.35\nheight = 1.8\n\n[sub_resource type=\"SphereShape3D\" id=\"SphereShape_probe\"]\nradius = 0.35\n\n[sub_resource type=\"BoxShape3D\" id=\"BoxShape_floor\"]\nsize = Vector3(40, 0.2, 40)\n\n[sub_resource type=\"StandardMaterial3D\" id=\"StandardMaterial_floor\"]\nalbedo_color = Color(0.08, 0.11, 0.16, 1)\nmetallic = 0.2\nroughness = 0.75\n\n[sub_resource type=\"BoxMesh\" id=\"BoxMesh_floor\"]\nsize = Vector3(40, 0.2, 40)\n\n[node name=\"PlayerMovementTest\" type=\"Node3D\" unique_id=224180014]\n\n[node name=\"WorldEnvironment\" type=\"WorldEnvironment\" parent=\".\" unique_id=1166585754]\n\n[node name=\"DirectionalLight3D\" type=\"DirectionalLight3D\" parent=\".\" unique_id=1833943505]\ntransform = Transform3D(-1, 8.742278e-08, -1.0425061e-15, 0, 1.1924881e-08, 1, 8.742278e-08, 1, -1.1924881e-08, 0, 0, 0)\nshadow_enabled = true\nshadow_bias = 0.0\n\n[node name=\"Player\" type=\"CharacterBody3D\" parent=\".\" unique_id=2029457343]\nscript = ExtResource(\"1_player\")\n\n[node name=\"CollisionShape3D\" type=\"CollisionShape3D\" parent=\"Player\" unique_id=1089854119]\ntransform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 0.9, 0)\nshape = SubResource(\"CapsuleShape_player\")\n\n[node name=\"GroundProbe\" type=\"ShapeCast3D\" parent=\"Player\" unique_id=1366928150]\ntransform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 0.35, 0)\nshape = SubResource(\"SphereShape_probe\")\ntarget_position = Vector3(0, -0.2, 0)\n\n[node name=\"Head\" type=\"Node3D\" parent=\"Player\" unique_id=759020528]\ntransform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 1.6, 0)\n\n[node name=\"FirstPersonCamera\" type=\"Camera3D\" parent=\"Player/Head\" unique_id=1631382468]\ntransform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 0.06, -0.09)\ncurrent = true\n\n[node name=\"Armature_Arms\" parent=\"Player/Head/FirstPersonCamera\" unique_id=1710313489 instance=ExtResource(\"3_arms\")]\ntransform = Transform3D(-1, 0, 8.742278e-08, 0, 1, 0, -8.742278e-08, 0, -1, -0.011, -1.67, 0.09)\nscript = ExtResource(\"4_arms\")\n\n[node name=\"Armature_Body\" type=\"MeshInstance3D\" parent=\"Player\" unique_id=170914297]\n\n[node name=\"Armature_Legs\" type=\"MeshInstance3D\" parent=\"Player\" unique_id=2117488143]\n\n[node name=\"AnimationPlayer\" type=\"AnimationPlayer\" parent=\"Player\" unique_id=1312267907]\n\n[node name=\"AnimationTree\" type=\"AnimationTree\" parent=\"Player\" unique_id=2142222891]\nanim_player = NodePath(\"../AnimationPlayer\")\n\n[node name=\"AnimationController\" type=\"Node\" parent=\"Player\" unique_id=1230906331]\nscript = ExtResource(\"2_animation\")\n\n[node name=\"Floor\" type=\"StaticBody3D\" parent=\".\" unique_id=877254511]\n\n[node name=\"CollisionShape3D\" type=\"CollisionShape3D\" parent=\"Floor\" unique_id=1654409495]\ntransform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, -0.1, 0)\nshape = SubResource(\"BoxShape_floor\")\n\n[node name=\"MeshInstance3D\" type=\"MeshInstance3D\" parent=\"Floor\" unique_id=1448104210]\ntransform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, -0.1, 0)\nmaterial_override = SubResource(\"StandardMaterial_floor\")\nmesh = SubResource(\"BoxMesh_floor\")\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/scenes/PlayerMovementTest.tscn b/scenes/PlayerMovementTest.tscn
--- a/scenes/PlayerMovementTest.tscn	(revision 774c917d83a8063256c70186039af4df904bff27)
+++ b/scenes/PlayerMovementTest.tscn	(date 1789545027547)
@@ -2,12 +2,10 @@
 
 [ext_resource type="Script" uid="uid://pikamp6vhtto" path="res://scripts/player/PlayerMovement.cs" id="1_player"]
 [ext_resource type="Script" uid="uid://nqxihnrb77iw" path="res://scripts/player/PlayerAnimationController.cs" id="2_animation"]
-[ext_resource type="PackedScene" uid="uid://8ice2iun26gc" path="res://art/characters/Armature/Model/Armature_arms.fbx" id="3_arms"]
-[ext_resource type="Script" uid="uid://cqb0f2qba4y6w" path="res://scripts/player/FirstPersonArms.cs" id="4_arms"]
+[ext_resource type="PackedScene" uid="uid://cvu4g1q7u08cn" path="res://art/characters/manny.glb" id="3_arms"]
 
 [sub_resource type="CapsuleShape3D" id="CapsuleShape_player"]
-radius = 0.35
-height = 1.8
+height = 3.9279785
 
 [sub_resource type="SphereShape3D" id="SphereShape_probe"]
 radius = 0.35
@@ -28,7 +26,7 @@
 [node name="WorldEnvironment" type="WorldEnvironment" parent="." unique_id=1166585754]
 
 [node name="DirectionalLight3D" type="DirectionalLight3D" parent="." unique_id=1833943505]
-transform = Transform3D(-1, 8.742278e-08, -1.0425061e-15, 0, 1.1924881e-08, 1, 8.742278e-08, 1, -1.1924881e-08, 0, 0, 0)
+transform = Transform3D(-1, 8.742278e-08, -1.0425061e-15, 0, 1.1924881e-08, 1, 8.742278e-08, 1, -1.1924881e-08, 0, 1.3551311, 0)
 shadow_enabled = true
 shadow_bias = 0.0
 
@@ -36,7 +34,7 @@
 script = ExtResource("1_player")
 
 [node name="CollisionShape3D" type="CollisionShape3D" parent="Player" unique_id=1089854119]
-transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 0.9, 0)
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, -0.051631212, 1.9052796, -0.00052952766)
 shape = SubResource("CapsuleShape_player")
 
 [node name="GroundProbe" type="ShapeCast3D" parent="Player" unique_id=1366928150]
@@ -44,20 +42,16 @@
 shape = SubResource("SphereShape_probe")
 target_position = Vector3(0, -0.2, 0)
 
+[node name="Armature" parent="Player" unique_id=1449932736 instance=ExtResource("3_arms")]
+
 [node name="Head" type="Node3D" parent="Player" unique_id=759020528]
 transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 1.6, 0)
 
 [node name="FirstPersonCamera" type="Camera3D" parent="Player/Head" unique_id=1631382468]
-transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 0.06, -0.09)
+transform = Transform3D(-0.99904203, 4.6036305e-10, -0.04376243, -0.0023720246, 0.99853, 0.05415035, 0.043698106, 0.054202266, -0.99757344, 0, 1.9999999, 0.2)
 current = true
-
-[node name="Armature_Arms" parent="Player/Head/FirstPersonCamera" unique_id=1710313489 instance=ExtResource("3_arms")]
-transform = Transform3D(-1, 0, 8.742278e-08, 0, 1, 0, -8.742278e-08, 0, -1, -0.011, -1.67, 0.09)
-script = ExtResource("4_arms")
-
-[node name="Armature_Body" type="MeshInstance3D" parent="Player" unique_id=170914297]
-
-[node name="Armature_Legs" type="MeshInstance3D" parent="Player" unique_id=2117488143]
+fov = 92.8
+near = 10.0
 
 [node name="AnimationPlayer" type="AnimationPlayer" parent="Player" unique_id=1312267907]
 
@@ -74,6 +68,6 @@
 shape = SubResource("BoxShape_floor")
 
 [node name="MeshInstance3D" type="MeshInstance3D" parent="Floor" unique_id=1448104210]
-transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, -0.1, 0)
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, -0.08874316, 0)
 material_override = SubResource("StandardMaterial_floor")
 mesh = SubResource("BoxMesh_floor")
Index: scripts/player/PlayerMovement.cs
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>using Godot;\nusing System;\n\n/// <summary>\n/// Local-only player locomotion. Network prediction/synchronisation belongs in a later layer.\n/// </summary>\npublic partial class PlayerMovement : CharacterBody3D\n{\n    public enum MovementState { Idle, Walk, Sprint, Crouch, CrouchWalk, Slide, Jump, Fall, Aim }\n\n    public const float WalkSpeed = 4.7f;\n    public const float SprintSpeed = 7.2f;\n    public const float CrouchSpeed = 2.3f;\n    public const float Gravity = -18.0f;\n    public const float JumpVelocity = 6.5727f;\n    public const float TerminalFallVelocity = -53.0f;\n    public const float SettledGroundVelocity = -3.75f;\n\n    private const float StandingCapsuleHeight = 1.8f;\n    private const float CrouchedCapsuleHeight = 1.0f;\n    private const float HeightTransitionSeconds = 0.2f;\n    private const float SpeedTransitionSeconds = 0.12f;\n    private const float AirControlMultiplier = 0.65f;\n    private const float SlideSpeedMultiplier = 1.3f;\n    private const float MaximumSlideSpeed = 9.4f;\n    private const float SlideFriction = 10.0f;\n    private const float LandingCooldownSeconds = 0.15f;\n\n    [Export] public float MouseSensitivity { get; set; } = 0.0025f;\n    [Export] public float CameraPitchLimitDegrees { get; set; } = 85.0f;\n\n    public MovementState State { get; private set; } = MovementState.Fall;\n    // These are intentionally replicated by MultiplayerSynchronizer.  Movement remains client-authoritative.\n    [Export] public string ReplicatedMovementState { get; set; } = MovementState.Idle.ToString();\n    [Export] public bool ReplicatedAimState { get; set; }\n\n    private CollisionShape3D _collisionShape = null!;\n    private ShapeCast3D _groundProbe = null!;\n    private Node3D _head = null!;\n    private Camera3D _camera = null!;\n    private PlayerAnimationController _animationController = null!;\n    private Health? _health;\n    private CapsuleShape3D _capsule = null!;\n    private Vector3 _slideVelocity;\n    private float _pitch;\n    private float _landingCooldown;\n    private bool _wasGrounded;\n\n    public override void _Ready()\n    {\n        _collisionShape = GetNode<CollisionShape3D>(\"CollisionShape3D\");\n        _groundProbe = GetNode<ShapeCast3D>(\"GroundProbe\");\n        _head = GetNode<Node3D>(\"Head\");\n        _camera = GetNode<Camera3D>(\"Head/FirstPersonCamera\");\n        _animationController = GetNode<PlayerAnimationController>(\"AnimationController\");\n        _health = GetNodeOrNull<Health>(\"Health\");\n        _capsule = (CapsuleShape3D)_collisionShape.Shape;\n        ((SphereShape3D)_groundProbe.Shape).Radius = _capsule.Radius;\n        if (IsMultiplayerAuthority()) Input.MouseMode = Input.MouseModeEnum.Captured;\n    }\n\n    public override void _UnhandledInput(InputEvent @event)\n    {\n        if (!IsMultiplayerAuthority()) return;\n        if (@event is InputEventMouseMotion mouseMotion && Input.MouseMode == Input.MouseModeEnum.Captured)\n        {\n            RotateY(-mouseMotion.Relative.X * MouseSensitivity);\n            _pitch = Mathf.Clamp(_pitch - mouseMotion.Relative.Y * MouseSensitivity,\n                Mathf.DegToRad(-CameraPitchLimitDegrees), Mathf.DegToRad(CameraPitchLimitDegrees));\n            _head.Rotation = new Vector3(_pitch, 0.0f, 0.0f);\n        }\n\n        if (@event.IsActionPressed(\"ui_cancel\"))\n            Input.MouseMode = Input.MouseModeEnum.Visible;\n        else if (@event is InputEventMouseButton && Input.MouseMode == Input.MouseModeEnum.Visible)\n            Input.MouseMode = Input.MouseModeEnum.Captured;\n    }\n\n    public override void _PhysicsProcess(double deltaValue)\n    {\n        if (!IsMultiplayerAuthority())\n        {\n            if (Enum.TryParse(ReplicatedMovementState, out MovementState remoteState))\n                _animationController.SetMovementState(remoteState);\n            return;\n        }\n        if (_health != null && _health.IsDead)\n        {\n            Velocity = Vector3.Zero;\n            return;\n        }\n        float delta = (float)deltaValue;\n        bool grounded = IsOnFloor() || _groundProbe.IsColliding();\n        Vector2 moveInput = Input.GetVector(\"move_left\", \"move_right\", \"move_forward\", \"move_back\");\n        Vector3 moveDirection = (Transform.Basis * new Vector3(moveInput.X, 0.0f, moveInput.Y)).Normalized();\n        bool crouchHeld = Input.IsActionPressed(\"crouch\");\n\n        if (grounded && !_wasGrounded)\n            _landingCooldown = LandingCooldownSeconds;\n\n        UpdateVerticalVelocity(delta, grounded);\n\n        if (State == MovementState.Slide)\n            UpdateSlide(delta, moveDirection, crouchHeld);\n        else if (grounded)\n            UpdateGroundMovement(delta, moveInput, moveDirection, crouchHeld);\n        else\n            UpdateAirMovement(delta, moveDirection);\n\n        UpdateColliderAndHead(delta, State is MovementState.Crouch or MovementState.CrouchWalk or MovementState.Slide);\n        float targetFov = State == MovementState.Aim ? 55.0f : 75.0f;\n        _camera.Fov = Mathf.MoveToward(_camera.Fov, targetFov, 20.0f * delta / 0.15f);\n        MoveAndSlide();\n        _wasGrounded = IsOnFloor() || _groundProbe.IsColliding();\n        ReplicatedMovementState = State.ToString();\n        ReplicatedAimState = State == MovementState.Aim;\n        _animationController.SetMovementState(State);\n\n        // TODO: Add optional head bob, camera shake, and sprint FOV effects separately.\n    }\n\n    private void UpdateVerticalVelocity(float delta, bool grounded)\n    {\n        if (!grounded || _landingCooldown > 0.0f)\n        {\n            // Exponential terminal approach begins at exactly -18 m/s² from rest, without a hard clamp.\n            float terminalApproach = 1.0f - Mathf.Exp(-Mathf.Abs(Gravity / TerminalFallVelocity) * delta);\n            Velocity = new Vector3(Velocity.X, Mathf.Lerp(Velocity.Y, TerminalFallVelocity, terminalApproach), Velocity.Z);\n            if (grounded)\n                _landingCooldown = Mathf.Max(0.0f, _landingCooldown - delta);\n            if (!grounded && State is not MovementState.Jump)\n                SetState(MovementState.Fall);\n            return;\n        }\n\n        Velocity = new Vector3(Velocity.X, SettledGroundVelocity, Velocity.Z);\n    }\n\n    private void UpdateGroundMovement(float delta, Vector2 moveInput, Vector3 moveDirection, bool crouchHeld)\n    {\n        if (Input.IsActionJustPressed(\"crouch\") && State == MovementState.Sprint)\n        {\n            StartSlide(moveDirection);\n            return;\n        }\n\n        bool canJump = State is MovementState.Idle or MovementState.Walk or MovementState.Sprint or MovementState.Crouch or MovementState.Aim;\n        if (canJump && Input.IsActionJustPressed(\"jump\"))\n        {\n            Velocity = new Vector3(Velocity.X, JumpVelocity, Velocity.Z);\n            SetState(MovementState.Jump);\n            return;\n        }\n\n        bool hasMovement = moveInput.LengthSquared() > 0.001f;\n        bool forwardish = moveInput.Y < -0.1f;\n        bool sprinting = !crouchHeld && forwardish && Input.IsActionPressed(\"sprint\");\n        bool aiming = Input.IsActionPressed(\"aim\") && !sprinting;\n        float targetSpeed = crouchHeld ? CrouchSpeed : sprinting ? SprintSpeed : WalkSpeed;\n        ApplyHorizontalVelocity(moveDirection * targetSpeed, delta, 1.0f);\n\n        SetState(aiming ? MovementState.Aim : crouchHeld\n            ? (hasMovement ? MovementState.CrouchWalk : MovementState.Crouch)\n            : sprinting && hasMovement ? MovementState.Sprint\n            : hasMovement ? MovementState.Walk\n            : MovementState.Idle);\n    }\n\n    private void UpdateAirMovement(float delta, Vector3 moveDirection)\n    {\n        // Air has no independent speed limit; it only receives reduced acceleration toward input velocity.\n        ApplyHorizontalVelocity(moveDirection * WalkSpeed, delta, AirControlMultiplier);\n        SetState(Velocity.Y > 0.0f ? MovementState.Jump : MovementState.Fall);\n    }\n\n    private void StartSlide(Vector3 moveDirection)\n    {\n        Vector3 horizontal = new Vector3(Velocity.X, 0.0f, Velocity.Z);\n        Vector3 direction = horizontal.LengthSquared() > 0.001f ? horizontal.Normalized()\n            : moveDirection.LengthSquared() > 0.001f ? moveDirection : -Transform.Basis.Z;\n        float speed = Mathf.Min(horizontal.Length() * SlideSpeedMultiplier, MaximumSlideSpeed);\n        _slideVelocity = direction * speed;\n        SetState(MovementState.Slide);\n    }\n\n    private void UpdateSlide(float delta, Vector3 moveDirection, bool crouchHeld)\n    {\n        float speed = _slideVelocity.Length();\n        if (moveDirection.LengthSquared() > 0.001f && speed > 0.001f)\n        {\n            // Thirty-percent-strength steering retains the slide's existing heading.\n            Vector3 steeredDirection = _slideVelocity.Normalized().Lerp(moveDirection, 0.3f * delta * 10.0f).Normalized();\n            _slideVelocity = steeredDirection * speed;\n        }\n\n        speed = Mathf.MoveToward(speed, 0.0f, SlideFriction * delta);\n        _slideVelocity = _slideVelocity.Normalized() * speed;\n        Velocity = new Vector3(_slideVelocity.X, Velocity.Y, _slideVelocity.Z);\n\n        if (speed < CrouchSpeed)\n            SetState(crouchHeld ? MovementState.Crouch : MovementState.Idle);\n    }\n\n    private void ApplyHorizontalVelocity(Vector3 targetVelocity, float delta, float accelerationMultiplier)\n    {\n        Vector3 horizontal = new(Velocity.X, 0.0f, Velocity.Z);\n        float transitionSpeed = Mathf.Max(horizontal.Length(), targetVelocity.Length()) / SpeedTransitionSeconds;\n        horizontal = horizontal.MoveToward(targetVelocity, transitionSpeed * accelerationMultiplier * delta);\n        Velocity = new Vector3(horizontal.X, Velocity.Y, horizontal.Z);\n    }\n\n    private void UpdateColliderAndHead(float delta, bool crouching)\n    {\n        float targetHeight = crouching ? CrouchedCapsuleHeight : StandingCapsuleHeight;\n        _capsule.Height = Mathf.MoveToward(_capsule.Height, targetHeight, (StandingCapsuleHeight - CrouchedCapsuleHeight) * delta / HeightTransitionSeconds);\n        _collisionShape.Position = new Vector3(0.0f, _capsule.Height * 0.5f, 0.0f);\n        _head.Position = new Vector3(0.0f, _capsule.Height - 0.2f, 0.0f);\n    }\n\n    private void SetState(MovementState state)\n    {\n        if (State == state) return;\n        State = state;\n        _animationController.SetMovementState(state);\n    }\n}\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/scripts/player/PlayerMovement.cs b/scripts/player/PlayerMovement.cs
--- a/scripts/player/PlayerMovement.cs	(revision 774c917d83a8063256c70186039af4df904bff27)
+++ b/scripts/player/PlayerMovement.cs	(date 1789544127507)
@@ -56,6 +56,9 @@
         _health = GetNodeOrNull<Health>("Health");
         _capsule = (CapsuleShape3D)_collisionShape.Shape;
         ((SphereShape3D)_groundProbe.Shape).Radius = _capsule.Radius;
+        // FirstPersonArms used to own this; with a single always-visible body mesh there is no
+        // separate view-model to toggle, but each client must still only activate its own camera.
+        _camera.Current = IsMultiplayerAuthority();
         if (IsMultiplayerAuthority()) Input.MouseMode = Input.MouseModeEnum.Captured;
     }
 
Index: scripts/player/PlayerAnimationController.cs
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>using Godot;\nusing System.Collections.Generic;\n\n/// <summary>\n/// Builds the local player's locomotion AnimationLibrary and state machine from\n/// the imported Mixamo clips.  The clips remain as individual source assets so\n/// they can be reimported or retargeted without maintaining a second copy.\n/// </summary>\npublic partial class PlayerAnimationController : Node\n{\n    // 0.15 s was short enough to expose the very different first poses of Mixamo's clips.\n    private const float CrossfadeSeconds = 0.24f;\n\n    private static readonly Dictionary<PlayerMovement.MovementState, string> StateClips = new()\n    {\n        { PlayerMovement.MovementState.Idle, \"locomotion_idle\" },\n        { PlayerMovement.MovementState.Walk, \"locomotion_walk_fwd\" },\n        { PlayerMovement.MovementState.Sprint, \"locomotion_sprint_fwd\" },\n        { PlayerMovement.MovementState.Crouch, \"locomotion_crouch_idle\" },\n        { PlayerMovement.MovementState.CrouchWalk, \"locomotion_crouch_walk\" },\n        // No dedicated Mixamo slide was supplied; crouch-walk is the closest temporary clip.\n        { PlayerMovement.MovementState.Slide, \"locomotion_crouch_walk\" },\n        { PlayerMovement.MovementState.Jump, \"combat_jump\" },\n        { PlayerMovement.MovementState.Aim, \"combat_ads_idle\" },\n        // No dedicated fall was supplied; the looping jump clip is the closest temporary clip.\n        { PlayerMovement.MovementState.Fall, \"combat_jump\" },\n    };\n\n    private static readonly (string ClipName, string Path)[] ImportedClips =\n    {\n        (\"combat_ads_idle\", \"res://art/animations/player/combat/combat_ads_idle.fbx\"),\n        (\"combat_fire_rifle\", \"res://art/animations/player/combat/combat_fire_rifle.fbx\"),\n        (\"combat_fire_rifle_crouch\", \"res://art/animations/player/combat/combat_fire_rifle_crouch.fbx\"),\n        (\"combat_fire_rifle_crouch_walk\", \"res://art/animations/player/combat/combat_fire_rifle_crouch_walk.fbx\"),\n        (\"combat_fire_rifle_sprint\", \"res://art/animations/player/combat/combat_fire_rifle_sprint.fbx\"),\n        (\"combat_fire_rifle_walk\", \"res://art/animations/player/combat/combat_fire_rifle_walk.fbx\"),\n        (\"combat_holster_rifle\", \"res://art/animations/player/combat/combat_holster_rifle.fbx\"),\n        (\"locomotion_idle\", \"res://art/animations/player/locomotion/locomotion_idle.fbx\"),\n        (\"combat_jump_back\", \"res://art/animations/player/combat/combat_jump_back.fbx\"),\n        (\"locomotion_walk_fwd\", \"res://art/animations/player/locomotion/locomotion_walk_fwd.fbx\"),\n        (\"combat_jump_fwd\", \"res://art/animations/player/combat/combat_jump_fwd.fbx\"),\n        (\"combat_jump\", \"res://art/animations/player/combat/combat_jump.fbx\"),\n        (\"combat_jump_rifle\", \"res://art/animations/player/combat/combat_jump_rifle.fbx\"),\n        (\"combat_jump_vertical\", \"res://art/animations/player/combat/combat_jump_vertical.fbx\"),\n        (\"locomotion_sprint_fwd\", \"res://art/animations/player/locomotion/locomotion_sprint_fwd.fbx\"),\n        (\"combat_reload_rifle\", \"res://art/animations/player/combat/combat_reload_rifle.fbx\"),\n        (\"combat_reload_rifle_crouch\", \"res://art/animations/player/combat/combat_reload_rifle_crouch.fbx\"),\n        (\"combat_reload_rifle_sprint\", \"res://art/animations/player/combat/combat_reload_rifle_sprint.fbx\"),\n        (\"combat_reload_rifle_walk\", \"res://art/animations/player/combat/combat_reload_rifle_walk.fbx\"),\n        (\"combat_turn_left\", \"res://art/animations/player/combat/combat_turn_left.fbx\"),\n        (\"combat_turn_right\", \"res://art/animations/player/combat/combat_turn_right.fbx\"),\n        (\"locomotion_crouch_idle\", \"res://art/animations/player/locomotion/locomotion_crouch_idle.fbx\"),\n        (\"locomotion_crouch_walk\", \"res://art/animations/player/locomotion/locomotion_crouch_walk.fbx\"),\n        (\"locomotion_crouch_strafe_left\", \"res://art/animations/player/locomotion/locomotion_crouch_strafe_left.fbx\"),\n        (\"locomotion_crouch_strafe_right\", \"res://art/animations/player/locomotion/locomotion_crouch_strafe_right.fbx\"),\n        (\"locomotion_crouch_to_standing_rifle\", \"res://art/animations/player/locomotion/locomotion_crouch_to_standing_rifle.fbx\"),\n        (\"locomotion_crouch_walk_back\", \"res://art/animations/player/locomotion/locomotion_crouch_walk_back.fbx\"),\n        (\"locomotion_crouch_walk_back_left\", \"res://art/animations/player/locomotion/locomotion_crouch_walk_back_left.fbx\"),\n        (\"locomotion_crouch_walk_back_right\", \"res://art/animations/player/locomotion/locomotion_crouch_walk_back_right.fbx\"),\n        (\"locomotion_crouch_walk_fwd_left\", \"res://art/animations/player/locomotion/locomotion_crouch_walk_fwd_left.fbx\"),\n        (\"locomotion_crouch_walk_fwd_right\", \"res://art/animations/player/locomotion/locomotion_crouch_walk_fwd_right.fbx\"),\n        (\"locomotion_sprint_back\", \"res://art/animations/player/locomotion/locomotion_sprint_back.fbx\"),\n        (\"locomotion_sprint_back_left\", \"res://art/animations/player/locomotion/locomotion_sprint_back_left.fbx\"),\n        (\"locomotion_sprint_back_right\", \"res://art/animations/player/locomotion/locomotion_sprint_back_right.fbx\"),\n        (\"locomotion_sprint_fwd_left\", \"res://art/animations/player/locomotion/locomotion_sprint_fwd_left.fbx\"),\n        (\"locomotion_sprint_fwd_right\", \"res://art/animations/player/locomotion/locomotion_sprint_fwd_right.fbx\"),\n        (\"locomotion_stand_to_crouch\", \"res://art/animations/player/locomotion/locomotion_stand_to_crouch.fbx\"),\n        (\"locomotion_strafe_left\", \"res://art/animations/player/locomotion/locomotion_strafe_left.fbx\"),\n        (\"locomotion_strafe_right\", \"res://art/animations/player/locomotion/locomotion_strafe_right.fbx\"),\n        (\"locomotion_walk\", \"res://art/animations/player/locomotion/locomotion_walk.fbx\"),\n        (\"locomotion_walk_back\", \"res://art/animations/player/locomotion/locomotion_walk_back.fbx\"),\n        (\"locomotion_walk_fwd_left\", \"res://art/animations/player/locomotion/locomotion_walk_fwd_left.fbx\"),\n        (\"locomotion_walk_fwd_right\", \"res://art/animations/player/locomotion/locomotion_walk_fwd_right.fbx\"),\n        (\"death_01\", \"res://art/animations/player/death/death_01.fbx\"),\n        (\"death_02_back_headshot\", \"res://art/animations/player/death/death_02_back_headshot.fbx\"),\n        (\"death_03_front_headshot\", \"res://art/animations/player/death/death_03_front_headshot.fbx\"),\n        (\"death_04_back\", \"res://art/animations/player/death/death_04_back.fbx\"),\n        (\"death_05_front\", \"res://art/animations/player/death/death_05_front.fbx\"),\n    };\n\n    private AnimationPlayer _animationPlayer = null!;\n    private AnimationTree _animationTree = null!;\n    private AnimationNodeStateMachinePlayback _playback = null!;\n    private PlayerMovement.MovementState? _currentState;\n    private ulong _jumpAnimationHoldUntilMs;\n\n    public override void _Ready()\n    {\n        _animationPlayer = GetNode<AnimationPlayer>(\"../AnimationPlayer\");\n        _animationTree = GetNode<AnimationTree>(\"../AnimationTree\");\n        var player = GetParent<Node>();\n        var armsSkeleton = player.FindChild(\"GeneralSkeleton\", true, false) as Skeleton3D;\n        if (armsSkeleton == null)\n        {\n            GD.PushError(\"Player animation setup could not find the retargeted arms Skeleton3D.\");\n            return;\n        }\n        var armsSkeletonPath = player.GetPathTo(armsSkeleton);\n\n        var library = new AnimationLibrary();\n        foreach (var (clipName, path) in ImportedClips)\n            AddImportedClip(library, clipName, path, armsSkeletonPath);\n        _animationPlayer.AddAnimationLibrary(\"\", library);\n\n        var machine = new AnimationNodeStateMachine();\n        foreach (var (state, clipName) in StateClips)\n            machine.AddNode(state.ToString(), new AnimationNodeAnimation { Animation = new StringName(clipName) }, Vector2.Zero);\n\n        foreach (var (from, _) in StateClips)\n        foreach (var (to, _) in StateClips)\n        {\n            if (from == to) continue;\n            machine.AddTransition(from.ToString(), to.ToString(), new AnimationNodeStateMachineTransition\n            {\n                XfadeTime = CrossfadeSeconds,\n                Reset = false,\n            });\n        }\n\n        _animationTree.TreeRoot = machine;\n        _animationTree.Active = true;\n        _playback = (AnimationNodeStateMachinePlayback)_animationTree.Get(\"parameters/playback\");\n    }\n\n    public void SetMovementState(PlayerMovement.MovementState state)\n    {\n        if (state == PlayerMovement.MovementState.Fall && _currentState == PlayerMovement.MovementState.Jump\n            && Time.GetTicksMsec() < _jumpAnimationHoldUntilMs)\n            state = PlayerMovement.MovementState.Jump;\n        if (!IsInstanceValid(_playback) || _currentState == state) return;\n        _currentState = state;\n        if (state == PlayerMovement.MovementState.Jump)\n            _jumpAnimationHoldUntilMs = Time.GetTicksMsec() + 730;\n        // Do not use world movement speed to scale authored animation: it was making sprint 53%\n        // faster and compressing jump to 0.73 s. The source clips carry their own cadence.\n        _animationPlayer.SpeedScale = 1.0f;\n        _playback.Travel(state.ToString());\n    }\n\n    private static void AddImportedClip(AnimationLibrary library, string clipName, string path, NodePath targetSkeletonPath)\n    {\n        var scene = ResourceLoader.Load<PackedScene>(path);\n        var importedRoot = scene?.Instantiate();\n        var sourcePlayer = importedRoot?.FindChild(\"AnimationPlayer\", true, false) as AnimationPlayer;\n        var sourceLibrary = sourcePlayer?.GetAnimationLibrary(\"\");\n        var animationNames = sourceLibrary?.GetAnimationList();\n        if (animationNames != null && animationNames.Count > 0)\n        {\n            var animation = sourceLibrary!.GetAnimation(animationNames[0]);\n            if (animation != null)\n            {\n                var retargetedAnimation = (Animation)animation.Duplicate();\n                RebindTracksToArmsSkeleton(retargetedAnimation, targetSkeletonPath);\n                library.AddAnimation(clipName, retargetedAnimation);\n            }\n        }\n        importedRoot?.QueueFree();\n    }\n\n    private static void RebindTracksToArmsSkeleton(Animation animation, NodePath targetSkeletonPath)\n    {\n        for (int trackIndex = 0; trackIndex < animation.GetTrackCount(); trackIndex++)\n        {\n            string sourcePath = animation.TrackGetPath(trackIndex).ToString();\n            int boneSeparator = sourcePath.IndexOf(':');\n            if (boneSeparator < 0) continue;\n\n            string boneName = sourcePath[(boneSeparator + 1)..];\n            // The imported clips have already been retargeted by Godot's BoneMap: their tracks\n            // use the exact target names (Hips, LeftUpperArm, RightHand, …). Keep those names.\n            animation.TrackSetPath(trackIndex, new NodePath($\"{targetSkeletonPath}:{boneName}\"));\n        }\n    }\n}\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/scripts/player/PlayerAnimationController.cs b/scripts/player/PlayerAnimationController.cs
--- a/scripts/player/PlayerAnimationController.cs	(revision 774c917d83a8063256c70186039af4df904bff27)
+++ b/scripts/player/PlayerAnimationController.cs	(date 1789547326521)
@@ -2,173 +2,195 @@
 using System.Collections.Generic;
 
 /// <summary>
-/// Builds the local player's locomotion AnimationLibrary and state machine from
-/// the imported Mixamo clips.  The clips remain as individual source assets so
-/// they can be reimported or retargeted without maintaining a second copy.
+/// Builds the player's locomotion AnimationLibrary and state machine from three consolidated
+/// Blender/Mixamo exports (locomotion, combat, death), each carrying many named takes on the
+/// same skeleton as art/characters/manny.glb. Unlike the old per-clip FBX pipeline, clip names
+/// already match the state-machine keys used below - no per-clip renaming needed here.
 /// </summary>
 public partial class PlayerAnimationController : Node
 {
-    // 0.15 s was short enough to expose the very different first poses of Mixamo's clips.
-    private const float CrossfadeSeconds = 0.24f;
+	// 0.15 s was short enough to expose the very different first poses of Mixamo's clips.
+	private const float CrossfadeSeconds = 0.24f;
+	private const string DefaultDeathClip = "Death From The Front";
 
-    private static readonly Dictionary<PlayerMovement.MovementState, string> StateClips = new()
-    {
-        { PlayerMovement.MovementState.Idle, "locomotion_idle" },
-        { PlayerMovement.MovementState.Walk, "locomotion_walk_fwd" },
-        { PlayerMovement.MovementState.Sprint, "locomotion_sprint_fwd" },
-        { PlayerMovement.MovementState.Crouch, "locomotion_crouch_idle" },
-        { PlayerMovement.MovementState.CrouchWalk, "locomotion_crouch_walk" },
-        // No dedicated Mixamo slide was supplied; crouch-walk is the closest temporary clip.
-        { PlayerMovement.MovementState.Slide, "locomotion_crouch_walk" },
-        { PlayerMovement.MovementState.Jump, "combat_jump" },
-        { PlayerMovement.MovementState.Aim, "combat_ads_idle" },
-        // No dedicated fall was supplied; the looping jump clip is the closest temporary clip.
-        { PlayerMovement.MovementState.Fall, "combat_jump" },
-    };
+	private static readonly Dictionary<PlayerMovement.MovementState, string> StateClips = new()
+	{
+		{ PlayerMovement.MovementState.Idle, "locomotion_idle" },
+		{ PlayerMovement.MovementState.Walk, "locomotion_walk_fwd" },
+		{ PlayerMovement.MovementState.Sprint, "locomotion_sprint_fwd" },
+		{ PlayerMovement.MovementState.Crouch, "locomotion_crouch_idle" },
+		{ PlayerMovement.MovementState.CrouchWalk, "locomotion_crouch_walk" },
+		// No dedicated slide clip was supplied; crouch-walk remains the closest stand-in.
+		{ PlayerMovement.MovementState.Slide, "locomotion_crouch_walk" },
+		// combat_jump_loop is the sustained airborne pose, reused for both states since no
+		// separate fall clip exists. combat_jump_down looks like a landing clip - worth trying
+		// there instead once you can actually see these play back to back.
+		{ PlayerMovement.MovementState.Jump, "combat_jump_loop" },
+		{ PlayerMovement.MovementState.Fall, "combat_jump_loop" },
+		{ PlayerMovement.MovementState.Aim, "combat_ads_idle" },
+	};
 
-    private static readonly (string ClipName, string Path)[] ImportedClips =
-    {
-        ("combat_ads_idle", "res://art/animations/player/combat/combat_ads_idle.fbx"),
-        ("combat_fire_rifle", "res://art/animations/player/combat/combat_fire_rifle.fbx"),
-        ("combat_fire_rifle_crouch", "res://art/animations/player/combat/combat_fire_rifle_crouch.fbx"),
-        ("combat_fire_rifle_crouch_walk", "res://art/animations/player/combat/combat_fire_rifle_crouch_walk.fbx"),
-        ("combat_fire_rifle_sprint", "res://art/animations/player/combat/combat_fire_rifle_sprint.fbx"),
-        ("combat_fire_rifle_walk", "res://art/animations/player/combat/combat_fire_rifle_walk.fbx"),
-        ("combat_holster_rifle", "res://art/animations/player/combat/combat_holster_rifle.fbx"),
-        ("locomotion_idle", "res://art/animations/player/locomotion/locomotion_idle.fbx"),
-        ("combat_jump_back", "res://art/animations/player/combat/combat_jump_back.fbx"),
-        ("locomotion_walk_fwd", "res://art/animations/player/locomotion/locomotion_walk_fwd.fbx"),
-        ("combat_jump_fwd", "res://art/animations/player/combat/combat_jump_fwd.fbx"),
-        ("combat_jump", "res://art/animations/player/combat/combat_jump.fbx"),
-        ("combat_jump_rifle", "res://art/animations/player/combat/combat_jump_rifle.fbx"),
-        ("combat_jump_vertical", "res://art/animations/player/combat/combat_jump_vertical.fbx"),
-        ("locomotion_sprint_fwd", "res://art/animations/player/locomotion/locomotion_sprint_fwd.fbx"),
-        ("combat_reload_rifle", "res://art/animations/player/combat/combat_reload_rifle.fbx"),
-        ("combat_reload_rifle_crouch", "res://art/animations/player/combat/combat_reload_rifle_crouch.fbx"),
-        ("combat_reload_rifle_sprint", "res://art/animations/player/combat/combat_reload_rifle_sprint.fbx"),
-        ("combat_reload_rifle_walk", "res://art/animations/player/combat/combat_reload_rifle_walk.fbx"),
-        ("combat_turn_left", "res://art/animations/player/combat/combat_turn_left.fbx"),
-        ("combat_turn_right", "res://art/animations/player/combat/combat_turn_right.fbx"),
-        ("locomotion_crouch_idle", "res://art/animations/player/locomotion/locomotion_crouch_idle.fbx"),
-        ("locomotion_crouch_walk", "res://art/animations/player/locomotion/locomotion_crouch_walk.fbx"),
-        ("locomotion_crouch_strafe_left", "res://art/animations/player/locomotion/locomotion_crouch_strafe_left.fbx"),
-        ("locomotion_crouch_strafe_right", "res://art/animations/player/locomotion/locomotion_crouch_strafe_right.fbx"),
-        ("locomotion_crouch_to_standing_rifle", "res://art/animations/player/locomotion/locomotion_crouch_to_standing_rifle.fbx"),
-        ("locomotion_crouch_walk_back", "res://art/animations/player/locomotion/locomotion_crouch_walk_back.fbx"),
-        ("locomotion_crouch_walk_back_left", "res://art/animations/player/locomotion/locomotion_crouch_walk_back_left.fbx"),
-        ("locomotion_crouch_walk_back_right", "res://art/animations/player/locomotion/locomotion_crouch_walk_back_right.fbx"),
-        ("locomotion_crouch_walk_fwd_left", "res://art/animations/player/locomotion/locomotion_crouch_walk_fwd_left.fbx"),
-        ("locomotion_crouch_walk_fwd_right", "res://art/animations/player/locomotion/locomotion_crouch_walk_fwd_right.fbx"),
-        ("locomotion_sprint_back", "res://art/animations/player/locomotion/locomotion_sprint_back.fbx"),
-        ("locomotion_sprint_back_left", "res://art/animations/player/locomotion/locomotion_sprint_back_left.fbx"),
-        ("locomotion_sprint_back_right", "res://art/animations/player/locomotion/locomotion_sprint_back_right.fbx"),
-        ("locomotion_sprint_fwd_left", "res://art/animations/player/locomotion/locomotion_sprint_fwd_left.fbx"),
-        ("locomotion_sprint_fwd_right", "res://art/animations/player/locomotion/locomotion_sprint_fwd_right.fbx"),
-        ("locomotion_stand_to_crouch", "res://art/animations/player/locomotion/locomotion_stand_to_crouch.fbx"),
-        ("locomotion_strafe_left", "res://art/animations/player/locomotion/locomotion_strafe_left.fbx"),
-        ("locomotion_strafe_right", "res://art/animations/player/locomotion/locomotion_strafe_right.fbx"),
-        ("locomotion_walk", "res://art/animations/player/locomotion/locomotion_walk.fbx"),
-        ("locomotion_walk_back", "res://art/animations/player/locomotion/locomotion_walk_back.fbx"),
-        ("locomotion_walk_fwd_left", "res://art/animations/player/locomotion/locomotion_walk_fwd_left.fbx"),
-        ("locomotion_walk_fwd_right", "res://art/animations/player/locomotion/locomotion_walk_fwd_right.fbx"),
-        ("death_01", "res://art/animations/player/death/death_01.fbx"),
-        ("death_02_back_headshot", "res://art/animations/player/death/death_02_back_headshot.fbx"),
-        ("death_03_front_headshot", "res://art/animations/player/death/death_03_front_headshot.fbx"),
-        ("death_04_back", "res://art/animations/player/death/death_04_back.fbx"),
-        ("death_05_front", "res://art/animations/player/death/death_05_front.fbx"),
-    };
+	private static readonly string[] ClipSourceScenes =
+	{
+		"res://art/animations/player/locomotion/player animation locomotion.glb",
+		"res://art/animations/player/combat/player animation combat.glb",
+		"res://art/animations/player/death/player animation death.glb",
+	};
 
-    private AnimationPlayer _animationPlayer = null!;
-    private AnimationTree _animationTree = null!;
-    private AnimationNodeStateMachinePlayback _playback = null!;
-    private PlayerMovement.MovementState? _currentState;
-    private ulong _jumpAnimationHoldUntilMs;
+	private AnimationPlayer _animationPlayer = null!;
+	private AnimationTree _animationTree = null!;
+	private AnimationNodeStateMachinePlayback _playback = null!;
+	private PlayerMovement.MovementState? _currentState;
+	private ulong _jumpAnimationHoldUntilMs;
+	private bool _isDead;
 
-    public override void _Ready()
-    {
-        _animationPlayer = GetNode<AnimationPlayer>("../AnimationPlayer");
-        _animationTree = GetNode<AnimationTree>("../AnimationTree");
-        var player = GetParent<Node>();
-        var armsSkeleton = player.FindChild("GeneralSkeleton", true, false) as Skeleton3D;
-        if (armsSkeleton == null)
-        {
-            GD.PushError("Player animation setup could not find the retargeted arms Skeleton3D.");
-            return;
-        }
-        var armsSkeletonPath = player.GetPathTo(armsSkeleton);
+	public override void _Ready()
+	{
+		_animationPlayer = GetNode<AnimationPlayer>("../AnimationPlayer");
+		_animationTree = GetNode<AnimationTree>("../AnimationTree");
+		var player = GetParent();
+		var skeleton = FindSkeleton(player);
+		if (skeleton == null)
+		{
+			GD.PushError("Player animation setup could not find a Skeleton3D under the player.");
+			return;
+		}
+		var skeletonPath = player.GetPathTo(skeleton);
 
-        var library = new AnimationLibrary();
-        foreach (var (clipName, path) in ImportedClips)
-            AddImportedClip(library, clipName, path, armsSkeletonPath);
-        _animationPlayer.AddAnimationLibrary("", library);
+		var library = new AnimationLibrary();
+		foreach (string scenePath in ClipSourceScenes)
+			ImportClipsFrom(scenePath, library, skeletonPath);
+		_animationPlayer.AddAnimationLibrary("", library);
 
-        var machine = new AnimationNodeStateMachine();
-        foreach (var (state, clipName) in StateClips)
-            machine.AddNode(state.ToString(), new AnimationNodeAnimation { Animation = new StringName(clipName) }, Vector2.Zero);
+		var machine = new AnimationNodeStateMachine();
+		var addedStates = new HashSet<PlayerMovement.MovementState>();
+		Godot.Collections.Array<StringName>? availableClipsForError = null;
+		foreach (var (state, clipName) in StateClips)
+		{
+			if (!library.HasAnimation(clipName))
+			{
+				// Godot's importer can rename things in ways I can't verify without running the
+				// importer myself (this already happened to bone names in WeaponAttachment) - so
+				// log what's actually in the library instead of guessing at another name.
+				availableClipsForError ??= library.GetAnimationList();
+				GD.PushError($"Animation clip '{clipName}' for state {state} was not found. "
+					+ $"Animations actually present: {string.Join(", ", availableClipsForError)}");
+				continue;
+			}
+			machine.AddNode(state.ToString(), new AnimationNodeAnimation { Animation = new StringName(clipName) }, Vector2.Zero);
+			addedStates.Add(state);
+		}
 
-        foreach (var (from, _) in StateClips)
-        foreach (var (to, _) in StateClips)
-        {
-            if (from == to) continue;
-            machine.AddTransition(from.ToString(), to.ToString(), new AnimationNodeStateMachineTransition
-            {
-                XfadeTime = CrossfadeSeconds,
-                Reset = false,
-            });
-        }
+		foreach (var from in addedStates)
+		foreach (var to in addedStates)
+		{
+			if (from == to) continue;
+			machine.AddTransition(from.ToString(), to.ToString(), new AnimationNodeStateMachineTransition
+			{
+				XfadeTime = CrossfadeSeconds,
+				Reset = false,
+			});
+		}
 
-        _animationTree.TreeRoot = machine;
-        _animationTree.Active = true;
-        _playback = (AnimationNodeStateMachinePlayback)_animationTree.Get("parameters/playback");
-    }
+		_animationTree.TreeRoot = machine;
+		_animationTree.Active = true;
+		_playback = (AnimationNodeStateMachinePlayback)_animationTree.Get("parameters/playback");
+	}
 
-    public void SetMovementState(PlayerMovement.MovementState state)
-    {
-        if (state == PlayerMovement.MovementState.Fall && _currentState == PlayerMovement.MovementState.Jump
-            && Time.GetTicksMsec() < _jumpAnimationHoldUntilMs)
-            state = PlayerMovement.MovementState.Jump;
-        if (!IsInstanceValid(_playback) || _currentState == state) return;
-        _currentState = state;
-        if (state == PlayerMovement.MovementState.Jump)
-            _jumpAnimationHoldUntilMs = Time.GetTicksMsec() + 730;
-        // Do not use world movement speed to scale authored animation: it was making sprint 53%
-        // faster and compressing jump to 0.73 s. The source clips carry their own cadence.
-        _animationPlayer.SpeedScale = 1.0f;
-        _playback.Travel(state.ToString());
-    }
+	public void SetMovementState(PlayerMovement.MovementState state)
+	{
+		if (_isDead) return;
+		if (state == PlayerMovement.MovementState.Fall && _currentState == PlayerMovement.MovementState.Jump
+			&& Time.GetTicksMsec() < _jumpAnimationHoldUntilMs)
+			state = PlayerMovement.MovementState.Jump;
+		if (!IsInstanceValid(_playback) || _currentState == state) return;
+		_currentState = state;
+		if (state == PlayerMovement.MovementState.Jump)
+			_jumpAnimationHoldUntilMs = Time.GetTicksMsec() + 730;
+		// Do not use world movement speed to scale authored animation: it was making sprint 53%
+		// faster and compressing jump to 0.73 s. The source clips carry their own cadence.
+		_animationPlayer.SpeedScale = 1.0f;
+		_playback.Travel(state.ToString());
+	}
 
-    private static void AddImportedClip(AnimationLibrary library, string clipName, string path, NodePath targetSkeletonPath)
-    {
-        var scene = ResourceLoader.Load<PackedScene>(path);
-        var importedRoot = scene?.Instantiate();
-        var sourcePlayer = importedRoot?.FindChild("AnimationPlayer", true, false) as AnimationPlayer;
-        var sourceLibrary = sourcePlayer?.GetAnimationLibrary("");
-        var animationNames = sourceLibrary?.GetAnimationList();
-        if (animationNames != null && animationNames.Count > 0)
-        {
-            var animation = sourceLibrary!.GetAnimation(animationNames[0]);
-            if (animation != null)
-            {
-                var retargetedAnimation = (Animation)animation.Duplicate();
-                RebindTracksToArmsSkeleton(retargetedAnimation, targetSkeletonPath);
-                library.AddAnimation(clipName, retargetedAnimation);
-            }
-        }
-        importedRoot?.QueueFree();
-    }
+	/// <summary>
+	/// Leaves the locomotion state machine and plays a fixed death pose once. Call from
+	/// Health.Died. Picking the clip by hit direction/headshot is a later refinement - it needs
+	/// attacker-relative data that WeaponSwitcher's hit RPC doesn't carry yet.
+	/// </summary>
+	public void PlayDeath()
+	{
+		_isDead = true;
+		_animationTree.Active = false;
+		_animationPlayer.Play(DefaultDeathClip);
+	}
+
+	/// <summary>
+	/// Call from Health.Respawned to hand control back to the locomotion state machine.
+	/// Clears the cached state so the next SetMovementState call always takes effect, even if
+	/// it happens to match whatever state was active at the moment of death.
+	/// </summary>
+	public void ResetAfterRespawn()
+	{
+		_isDead = false;
+		_currentState = null;
+		_animationTree.Active = true;
+		SetMovementState(PlayerMovement.MovementState.Idle);
+	}
+
+	private static Skeleton3D? FindSkeleton(Node root)
+	{
+		if (root is Skeleton3D skeleton) return skeleton;
+		foreach (Node child in root.GetChildren())
+		{
+			var found = FindSkeleton(child);
+			if (found != null) return found;
+		}
+		return null;
+	}
+
+	private static void ImportClipsFrom(string scenePath, AnimationLibrary library, NodePath targetSkeletonPath)
+	{
+		var scene = ResourceLoader.Load<PackedScene>(scenePath);
+		var importedRoot = scene?.Instantiate();
+		var sourcePlayer = importedRoot?.FindChild("AnimationPlayer", true, false) as AnimationPlayer;
+		if (sourcePlayer == null)
+		{
+			GD.PushError($"No AnimationPlayer found in {scenePath}.");
+			importedRoot?.QueueFree();
+			return;
+		}
+
+		// Pull from every library the file has rather than assuming the default "" one, since
+		// that assumption (and a hardcoded resource path) have both broken this project before.
+		foreach (StringName libraryName in sourcePlayer.GetAnimationLibraryList())
+		{
+			var sourceLibrary = sourcePlayer.GetAnimationLibrary(libraryName);
+			foreach (StringName clipName in sourceLibrary.GetAnimationList())
+			{
+				var animation = sourceLibrary.GetAnimation(clipName);
+				if (animation == null) continue;
+				var retargeted = (Animation)animation.Duplicate();
+				RebindTracksToSkeleton(retargeted, targetSkeletonPath);
+				if (library.HasAnimation(clipName)) library.RemoveAnimation(clipName);
+				library.AddAnimation(clipName, retargeted);
+			}
+		}
+		importedRoot?.QueueFree();
+	}
 
-    private static void RebindTracksToArmsSkeleton(Animation animation, NodePath targetSkeletonPath)
-    {
-        for (int trackIndex = 0; trackIndex < animation.GetTrackCount(); trackIndex++)
-        {
-            string sourcePath = animation.TrackGetPath(trackIndex).ToString();
-            int boneSeparator = sourcePath.IndexOf(':');
-            if (boneSeparator < 0) continue;
+	private static void RebindTracksToSkeleton(Animation animation, NodePath targetSkeletonPath)
+	{
+		for (int trackIndex = 0; trackIndex < animation.GetTrackCount(); trackIndex++)
+		{
+			string sourcePath = animation.TrackGetPath(trackIndex).ToString();
+			int boneSeparator = sourcePath.IndexOf(':');
+			if (boneSeparator < 0) continue;
 
-            string boneName = sourcePath[(boneSeparator + 1)..];
-            // The imported clips have already been retargeted by Godot's BoneMap: their tracks
-            // use the exact target names (Hips, LeftUpperArm, RightHand, …). Keep those names.
-            animation.TrackSetPath(trackIndex, new NodePath($"{targetSkeletonPath}:{boneName}"));
-        }
-    }
+			// Bone names themselves contain a colon ("mixamorig:Hips"), so only the FIRST colon
+			// is a separator; everything after it is the real bone name, embedded colon and all.
+			string boneName = sourcePath[(boneSeparator + 1)..];
+			animation.TrackSetPath(trackIndex, new NodePath($"{targetSkeletonPath}:{boneName}"));
+		}
+	}
 }
Index: scripts/player/WeaponAttachment.cs
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>using Godot;\n\n/// <summary>Attaches the active registry weapon to the shared visible player rig.</summary>\npublic partial class WeaponAttachment : Node3D\n{\n    private const string RegistryPath = \"res://scripts/data/WeaponRegistry.tres\";\n    private AudioStreamPlayer3D? _fireAudio;\n    private AudioStreamPlayer3D? _reloadAudio;\n\n    public override void _Ready()\n    {\n        Equip(ResourceLoader.Load<WeaponRegistry>(RegistryPath)?.GetActiveWeapon());\n    }\n\n    /// <summary>Single attachment path used for initial equip and hot switching.</summary>\n    public void Equip(WeaponData? weapon)\n    {\n        var oldAttachment = GetParent().FindChild(\"WeaponBoneAttachment\", true, false);\n        if (oldAttachment != null)\n        {\n            // Remove immediately before creating the replacement. QueueFree alone leaves a\n            // same-named BoneAttachment in the tree until frame end and can make a rapid switch\n            // resolve the outgoing attachment instead of the new hand attachment.\n            oldAttachment.GetParent()?.RemoveChild(oldAttachment);\n            oldAttachment.QueueFree();\n        }\n        _fireAudio = null;\n        _reloadAudio = null;\n        if (weapon == null || string.IsNullOrEmpty(weapon.ModelScenePath))\n        {\n            GD.PushError(\"Weapon attachment could not resolve an active weapon from WeaponRegistry.\");\n            return;\n        }\n\n        // Armature_Mesh (remote body) and Armature_Mesh_ArmsOnly (local view) are both skinned\n        // to this one GeneralSkeleton. The attachment must therefore live under this shared rig,\n        // rather than under either MeshInstance3D.\n        var skeleton = GetParent().FindChild(\"GeneralSkeleton\", true, false) as Skeleton3D;\n        if (skeleton == null || skeleton.FindBone(\"Right_Hand_Attach\") < 0)\n        {\n            GD.PushError(\"Weapon attachment could not find the arms Right_Hand_Attach bone.\");\n            return;\n        }\n\n        var attachment = new BoneAttachment3D { Name = \"WeaponBoneAttachment\", BoneName = \"Right_Hand_Attach\" };\n        skeleton.AddChild(attachment);\n\n        var weaponInstance = ResourceLoader.Load<PackedScene>(weapon.ModelScenePath)?.Instantiate<Node3D>();\n        if (weaponInstance == null)\n        {\n            GD.PushError($\"Weapon attachment could not load {weapon.ModelScenePath}.\");\n            attachment.QueueFree();\n            return;\n        }\n\n        weaponInstance.Name = \"EquippedWeapon\";\n        // BoneAttachment3D is already in Right_Hand_Attach space. The old arbitrary 6 cm\n        // forward offset made the grip visibly float off the hand in both views.\n        weaponInstance.Position = Vector3.Zero;\n        weaponInstance.Rotation = Vector3.Zero;\n        attachment.AddChild(weaponInstance);\n        AddAudioPlayers(attachment, weapon.WeaponName);\n    }\n\n    public void PlayFire() => _fireAudio?.Play();\n    public void PlayReload() => _reloadAudio?.Play();\n\n    private void AddAudioPlayers(Node attachment, string weaponName)\n    {\n        string firePath = weaponName switch\n        {\n            \"AK-47\" => \"res://audio/Clips/Weapons/Rifle/Shoot/Rifle_Shoot-001.wav\",\n            \"Shotgun\" => \"res://audio/Clips/Weapons/Shotgun/Shoot/Shotgun_Shoot-001.wav\",\n            \"Glock 19\" => \"res://audio/Clips/Weapons/Pistol/Shoot/Pistol_Shoot-001.wav\",\n            _ => string.Empty,\n        };\n        string reloadPath = weaponName switch\n        {\n            \"AK-47\" => \"res://audio/Clips/Weapons/Rifle/Reload/rifle_reload.wav\",\n            \"Shotgun\" => \"res://audio/Clips/Weapons/Shotgun/Reload/Shotgun_reload 1.wav\",\n            \"Glock 19\" => \"res://audio/Clips/Weapons/Pistol/Reload/Pistol_Reload.wav\",\n            _ => string.Empty,\n        };\n        _fireAudio = AddAudioPlayer(attachment, \"WeaponFireAudio\", firePath);\n        _reloadAudio = AddAudioPlayer(attachment, \"WeaponReloadAudio\", reloadPath);\n        if (weaponName == \"Karambit\") GD.Print(\"Karambit has no migrated swing/hit or reload audio; no audio player was assigned.\");\n    }\n\n    private static AudioStreamPlayer3D? AddAudioPlayer(Node parent, string name, string path)\n    {\n        if (string.IsNullOrEmpty(path)) return null;\n        var stream = ResourceLoader.Load<AudioStream>(path);\n        if (stream == null) { GD.PushError($\"Missing weapon audio: {path}\"); return null; }\n        var player = new AudioStreamPlayer3D { Name = name, Stream = stream, UnitSize = 8.0f };\n        parent.AddChild(player);\n        return player;\n    }\n}\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/scripts/player/WeaponAttachment.cs b/scripts/player/WeaponAttachment.cs
--- a/scripts/player/WeaponAttachment.cs	(revision 774c917d83a8063256c70186039af4df904bff27)
+++ b/scripts/player/WeaponAttachment.cs	(date 1789546987380)
@@ -1,98 +1,136 @@
 using Godot;
 
-/// <summary>Attaches the active registry weapon to the shared visible player rig.</summary>
+/// <summary>Attaches the active registry weapon to the shared visible player rig (the single
+/// "Manny" body mesh + "Armature" skeleton from manny.glb - there is no separate arms-only
+/// view-model anymore).</summary>
 public partial class WeaponAttachment : Node3D
 {
-    private const string RegistryPath = "res://scripts/data/WeaponRegistry.tres";
-    private AudioStreamPlayer3D? _fireAudio;
-    private AudioStreamPlayer3D? _reloadAudio;
+	private const string RegistryPath = "res://scripts/data/WeaponRegistry.tres";
+	// Godot's glTF importer sanitizes bone names on import (documented engine behavior - it does
+	// not preserve "mixamorig:RightHand" verbatim, and exactly how it mangles it isn't something
+	// I can verify without running the importer myself). Matching by substring instead of exact
+	// string survives whatever the actual sanitized form turns out to be.
+	private const string HandBoneNameFragment = "righthand";
+	private AudioStreamPlayer3D? _fireAudio;
+	private AudioStreamPlayer3D? _reloadAudio;
 
-    public override void _Ready()
-    {
-        Equip(ResourceLoader.Load<WeaponRegistry>(RegistryPath)?.GetActiveWeapon());
-    }
+	public override void _Ready()
+	{
+		Equip(ResourceLoader.Load<WeaponRegistry>(RegistryPath)?.GetActiveWeapon());
+	}
 
-    /// <summary>Single attachment path used for initial equip and hot switching.</summary>
-    public void Equip(WeaponData? weapon)
-    {
-        var oldAttachment = GetParent().FindChild("WeaponBoneAttachment", true, false);
-        if (oldAttachment != null)
-        {
-            // Remove immediately before creating the replacement. QueueFree alone leaves a
-            // same-named BoneAttachment in the tree until frame end and can make a rapid switch
-            // resolve the outgoing attachment instead of the new hand attachment.
-            oldAttachment.GetParent()?.RemoveChild(oldAttachment);
-            oldAttachment.QueueFree();
-        }
-        _fireAudio = null;
-        _reloadAudio = null;
-        if (weapon == null || string.IsNullOrEmpty(weapon.ModelScenePath))
-        {
-            GD.PushError("Weapon attachment could not resolve an active weapon from WeaponRegistry.");
-            return;
-        }
+	/// <summary>Single attachment path used for initial equip and hot switching.</summary>
+	public void Equip(WeaponData? weapon)
+	{
+		var oldAttachment = GetParent().FindChild("WeaponBoneAttachment", true, false);
+		if (oldAttachment != null)
+		{
+			// Remove immediately before creating the replacement. QueueFree alone leaves a
+			// same-named BoneAttachment in the tree until frame end and can make a rapid switch
+			// resolve the outgoing attachment instead of the new hand attachment.
+			oldAttachment.GetParent()?.RemoveChild(oldAttachment);
+			oldAttachment.QueueFree();
+		}
+		_fireAudio = null;
+		_reloadAudio = null;
+		if (weapon == null || string.IsNullOrEmpty(weapon.ModelScenePath))
+		{
+			GD.PushError("Weapon attachment could not resolve an active weapon from WeaponRegistry.");
+			return;
+		}
 
-        // Armature_Mesh (remote body) and Armature_Mesh_ArmsOnly (local view) are both skinned
-        // to this one GeneralSkeleton. The attachment must therefore live under this shared rig,
-        // rather than under either MeshInstance3D.
-        var skeleton = GetParent().FindChild("GeneralSkeleton", true, false) as Skeleton3D;
-        if (skeleton == null || skeleton.FindBone("Right_Hand_Attach") < 0)
-        {
-            GD.PushError("Weapon attachment could not find the arms Right_Hand_Attach bone.");
-            return;
-        }
+		// There's one shared "Manny" body mesh now (no separate remote/local view-model split),
+		// skinned to the "Armature" Skeleton3D that manny.glb imports as its scene root. Search
+		// by type rather than a hardcoded node name - a hardcoded name is exactly what broke
+		// weapon attachment twice already on this project.
+		var skeleton = (GetParent() as Skeleton3D) ?? FindSkeleton3D(GetParent());
+		int handBoneIndex = skeleton == null ? -1 : FindBoneContaining(skeleton, HandBoneNameFragment);
+		if (skeleton == null || handBoneIndex < 0)
+		{
+			string available = skeleton == null ? "(no Skeleton3D found)" : DumpBoneNames(skeleton);
+			GD.PushError($"Weapon attachment could not find a bone containing '{HandBoneNameFragment}'. Actual bones: {available}");
+			return;
+		}
+		string handBoneName = skeleton!.GetBoneName(handBoneIndex);
 
-        var attachment = new BoneAttachment3D { Name = "WeaponBoneAttachment", BoneName = "Right_Hand_Attach" };
-        skeleton.AddChild(attachment);
+		var attachment = new BoneAttachment3D { Name = "WeaponBoneAttachment", BoneName = handBoneName };
+		skeleton.AddChild(attachment);
 
-        var weaponInstance = ResourceLoader.Load<PackedScene>(weapon.ModelScenePath)?.Instantiate<Node3D>();
-        if (weaponInstance == null)
-        {
-            GD.PushError($"Weapon attachment could not load {weapon.ModelScenePath}.");
-            attachment.QueueFree();
-            return;
-        }
+		var weaponInstance = ResourceLoader.Load<PackedScene>(weapon.ModelScenePath)?.Instantiate<Node3D>();
+		if (weaponInstance == null)
+		{
+			GD.PushError($"Weapon attachment could not load {weapon.ModelScenePath}.");
+			attachment.QueueFree();
+			return;
+		}
 
-        weaponInstance.Name = "EquippedWeapon";
-        // BoneAttachment3D is already in Right_Hand_Attach space. The old arbitrary 6 cm
-        // forward offset made the grip visibly float off the hand in both views.
-        weaponInstance.Position = Vector3.Zero;
-        weaponInstance.Rotation = Vector3.Zero;
-        attachment.AddChild(weaponInstance);
-        AddAudioPlayers(attachment, weapon.WeaponName);
-    }
+		weaponInstance.Name = "EquippedWeapon";
+		// The old rig's Right_Hand_Attach bone had a purpose-built grip offset; the actual hand
+		// bone here doesn't. Zero is a starting point, not a verified value - expect to nudge
+		// Position/Rotation here once you can actually see the grip in-editor.
+		weaponInstance.Position = Vector3.Zero;
+		weaponInstance.Rotation = Vector3.Zero;
+		attachment.AddChild(weaponInstance);
+		AddAudioPlayers(attachment, weapon.WeaponName);
+	}
 
-    public void PlayFire() => _fireAudio?.Play();
-    public void PlayReload() => _reloadAudio?.Play();
+	public void PlayFire() => _fireAudio?.Play();
+	public void PlayReload() => _reloadAudio?.Play();
 
-    private void AddAudioPlayers(Node attachment, string weaponName)
-    {
-        string firePath = weaponName switch
-        {
-            "AK-47" => "res://audio/Clips/Weapons/Rifle/Shoot/Rifle_Shoot-001.wav",
-            "Shotgun" => "res://audio/Clips/Weapons/Shotgun/Shoot/Shotgun_Shoot-001.wav",
-            "Glock 19" => "res://audio/Clips/Weapons/Pistol/Shoot/Pistol_Shoot-001.wav",
-            _ => string.Empty,
-        };
-        string reloadPath = weaponName switch
-        {
-            "AK-47" => "res://audio/Clips/Weapons/Rifle/Reload/rifle_reload.wav",
-            "Shotgun" => "res://audio/Clips/Weapons/Shotgun/Reload/Shotgun_reload 1.wav",
-            "Glock 19" => "res://audio/Clips/Weapons/Pistol/Reload/Pistol_Reload.wav",
-            _ => string.Empty,
-        };
-        _fireAudio = AddAudioPlayer(attachment, "WeaponFireAudio", firePath);
-        _reloadAudio = AddAudioPlayer(attachment, "WeaponReloadAudio", reloadPath);
-        if (weaponName == "Karambit") GD.Print("Karambit has no migrated swing/hit or reload audio; no audio player was assigned.");
-    }
+	private static Skeleton3D? FindSkeleton3D(Node root)
+	{
+		if (root is Skeleton3D skeleton) return skeleton;
+		foreach (Node child in root.GetChildren())
+		{
+			var found = FindSkeleton3D(child);
+			if (found != null) return found;
+		}
+		return null;
+	}
+
+	private static int FindBoneContaining(Skeleton3D skeleton, string fragmentLower)
+	{
+		for (int i = 0; i < skeleton.GetBoneCount(); i++)
+			if (skeleton.GetBoneName(i).ToLowerInvariant().Contains(fragmentLower))
+				return i;
+		return -1;
+	}
+
+	private static string DumpBoneNames(Skeleton3D skeleton)
+	{
+		var names = new string[skeleton.GetBoneCount()];
+		for (int i = 0; i < names.Length; i++) names[i] = skeleton.GetBoneName(i);
+		return string.Join(", ", names);
+	}
+
+	private void AddAudioPlayers(Node attachment, string weaponName)
+	{
+		string firePath = weaponName switch
+		{
+			"AK-47" => "res://audio/Clips/Weapons/Rifle/Shoot/Rifle_Shoot-001.wav",
+			"Shotgun" => "res://audio/Clips/Weapons/Shotgun/Shoot/Shotgun_Shoot-001.wav",
+			"Glock 19" => "res://audio/Clips/Weapons/Pistol/Shoot/Pistol_Shoot-001.wav",
+			_ => string.Empty,
+		};
+		string reloadPath = weaponName switch
+		{
+			"AK-47" => "res://audio/Clips/Weapons/Rifle/Reload/rifle_reload.wav",
+			"Shotgun" => "res://audio/Clips/Weapons/Shotgun/Reload/Shotgun_reload 1.wav",
+			"Glock 19" => "res://audio/Clips/Weapons/Pistol/Reload/Pistol_Reload.wav",
+			_ => string.Empty,
+		};
+		_fireAudio = AddAudioPlayer(attachment, "WeaponFireAudio", firePath);
+		_reloadAudio = AddAudioPlayer(attachment, "WeaponReloadAudio", reloadPath);
+		if (weaponName == "Karambit") GD.Print("Karambit has no migrated swing/hit or reload audio; no audio player was assigned.");
+	}
 
-    private static AudioStreamPlayer3D? AddAudioPlayer(Node parent, string name, string path)
-    {
-        if (string.IsNullOrEmpty(path)) return null;
-        var stream = ResourceLoader.Load<AudioStream>(path);
-        if (stream == null) { GD.PushError($"Missing weapon audio: {path}"); return null; }
-        var player = new AudioStreamPlayer3D { Name = name, Stream = stream, UnitSize = 8.0f };
-        parent.AddChild(player);
-        return player;
-    }
+	private static AudioStreamPlayer3D? AddAudioPlayer(Node parent, string name, string path)
+	{
+		if (string.IsNullOrEmpty(path)) return null;
+		var stream = ResourceLoader.Load<AudioStream>(path);
+		if (stream == null) { GD.PushError($"Missing weapon audio: {path}"); return null; }
+		var player = new AudioStreamPlayer3D { Name = name, Stream = stream, UnitSize = 8.0f };
+		parent.AddChild(player);
+		return player;
+	}
 }
Index: scripts/player/WeaponSwitcher.cs
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>using Godot;\n\n/// <summary>\n/// Temporary test loadout. Owns ammo, fire-rate, and reload state for the equipped weapon and\n/// resolves hitscan fire against Health. Model attachment stays centralized in WeaponAttachment.\n/// </summary>\npublic partial class WeaponSwitcher : Node\n{\n    private const float MaxRange = 500.0f;\n    private const float MeleeRange = 2.5f;\n\n    private static readonly string[] Loadout = { \"AK-47\", \"Shotgun\", \"Glock 19\", \"Karambit\" };\n\n    private WeaponAttachment _attachment = null!;\n    private WeaponRegistry _registry = null!;\n    private Camera3D _camera = null!;\n    private CharacterBody3D _player = null!;\n    private Health? _health;\n\n    private WeaponData? _currentWeapon;\n    private int _currentAmmo;\n    private float _cooldownRemaining;\n    private float _reloadRemaining;\n\n    // Read-only surface for the HUD. Kept separate from the fields above so nothing outside\n    // this script can mutate combat state directly.\n    public string WeaponName => _currentWeapon?.WeaponName ?? string.Empty;\n    public int CurrentAmmo => _currentAmmo;\n    public int MagazineSize => _currentWeapon?.MagazineSize ?? 0;\n    public bool IsReloading => _reloadRemaining > 0.0f;\n\n    public override void _Ready()\n    {\n        _player = (CharacterBody3D)GetParent();\n        _attachment = GetNode<WeaponAttachment>(\"../Head/Armature_Arms/WeaponAttachment\");\n        _camera = GetNode<Camera3D>(\"../Head/FirstPersonCamera\");\n        _health = GetNodeOrNull<Health>(\"../Health\");\n        _registry = ResourceLoader.Load<WeaponRegistry>(\"res://scripts/data/WeaponRegistry.tres\")!;\n        Equip(0);\n    }\n\n    public override void _Process(double delta)\n    {\n        if (!_player.IsMultiplayerAuthority()) return;\n\n        for (int i = 0; i < Loadout.Length; i++)\n            if (Input.IsActionJustPressed($\"weapon_{i + 1}\")) Equip(i);\n\n        if (_health != null && _health.IsDead) return;\n\n        float step = (float)delta;\n        _cooldownRemaining = Mathf.Max(0.0f, _cooldownRemaining - step);\n        if (_reloadRemaining > 0.0f)\n        {\n            _reloadRemaining -= step;\n            if (_reloadRemaining <= 0.0f) FinishReload();\n        }\n\n        if (Input.IsActionJustPressed(\"reload\")) StartReload();\n        // Rate-limited by FireRateRPM below. Semi-auto weapons (Glock, Desert Eagle) should\n        // realistically require a fresh press per shot - not enforced yet, so holding fire\n        // will currently spam them at their listed cadence. Revisit alongside recoil.\n        if (Input.IsActionPressed(\"fire\") && _cooldownRemaining <= 0.0f && _reloadRemaining <= 0.0f)\n            TryFire();\n    }\n\n    private void Equip(int index)\n    {\n        foreach (var weapon in _registry.Weapons)\n        {\n            if (weapon.WeaponName != Loadout[index]) continue;\n            _currentWeapon = weapon;\n            _currentAmmo = weapon.MagazineSize;\n            _cooldownRemaining = 0.0f;\n            _reloadRemaining = 0.0f;\n            _attachment.Equip(weapon);\n            return;\n        }\n        GD.PushError($\"Test loadout weapon '{Loadout[index]}' is not registered.\");\n    }\n\n    private void TryFire()\n    {\n        if (_currentWeapon == null) return;\n        bool infiniteAmmo = _currentWeapon.MagazineSize <= 0;\n        if (!infiniteAmmo && _currentAmmo <= 0) { StartReload(); return; }\n\n        _cooldownRemaining = _currentWeapon.FireRateRPM > 0.0f ? 60.0f / _currentWeapon.FireRateRPM : 0.5f;\n        if (!infiniteAmmo) _currentAmmo--;\n        _attachment.PlayFire();\n        FireHitscan();\n    }\n\n    private void FireHitscan()\n    {\n        bool melee = _currentWeapon!.WeaponClass == \"Melee\";\n        float range = melee ? MeleeRange : MaxRange;\n        Vector3 from = _camera.GlobalPosition;\n        Vector3 to = from + (-_camera.GlobalTransform.Basis.Z) * range;\n\n        var query = PhysicsRayQueryParameters3D.Create(from, to,\n            exclude: new Godot.Collections.Array<Rid> { _player.GetRid() });\n        var result = _player.GetWorld3D().DirectSpaceState.IntersectRay(query);\n        if (result.Count == 0) return;\n\n        // TODO: distinguish head vs. body hits once a dedicated head hitbox exists on the rig;\n        // every hit currently counts as BodyDamage, and HeadshotDamage goes unused.\n        if (result[\"collider\"].As<Node>() is not CharacterBody3D body) return;\n        var targetHealth = body.GetNodeOrNull<Health>(\"Health\");\n        if (targetHealth == null || targetHealth == _health) return;\n\n        int attackerId = _player.GetMultiplayerAuthority();\n        int targetId = targetHealth.GetMultiplayerAuthority();\n        targetHealth.RpcId(targetId, Health.MethodName.ReceiveDamage, _currentWeapon.BodyDamage, attackerId);\n    }\n\n    private void StartReload()\n    {\n        if (_currentWeapon == null || _reloadRemaining > 0.0f) return;\n        bool infiniteAmmo = _currentWeapon.MagazineSize <= 0;\n        if (infiniteAmmo || _currentAmmo >= _currentWeapon.MagazineSize) return;\n        _reloadRemaining = _currentWeapon.ReloadTime > 0.0f ? _currentWeapon.ReloadTime : 0.1f;\n        _attachment.PlayReload();\n    }\n\n    private void FinishReload()\n    {\n        if (_currentWeapon != null) _currentAmmo = _currentWeapon.MagazineSize;\n        _reloadRemaining = 0.0f;\n    }\n}\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/scripts/player/WeaponSwitcher.cs b/scripts/player/WeaponSwitcher.cs
--- a/scripts/player/WeaponSwitcher.cs	(revision 774c917d83a8063256c70186039af4df904bff27)
+++ b/scripts/player/WeaponSwitcher.cs	(date 1789544382192)
@@ -6,126 +6,126 @@
 /// </summary>
 public partial class WeaponSwitcher : Node
 {
-    private const float MaxRange = 500.0f;
-    private const float MeleeRange = 2.5f;
+	private const float MaxRange = 500.0f;
+	private const float MeleeRange = 2.5f;
 
-    private static readonly string[] Loadout = { "AK-47", "Shotgun", "Glock 19", "Karambit" };
+	private static readonly string[] Loadout = { "AK-47", "Shotgun", "Glock 19", "Karambit" };
 
-    private WeaponAttachment _attachment = null!;
-    private WeaponRegistry _registry = null!;
-    private Camera3D _camera = null!;
-    private CharacterBody3D _player = null!;
-    private Health? _health;
+	private WeaponAttachment _attachment = null!;
+	private WeaponRegistry _registry = null!;
+	private Camera3D _camera = null!;
+	private CharacterBody3D _player = null!;
+	private Health? _health;
 
-    private WeaponData? _currentWeapon;
-    private int _currentAmmo;
-    private float _cooldownRemaining;
-    private float _reloadRemaining;
+	private WeaponData? _currentWeapon;
+	private int _currentAmmo;
+	private float _cooldownRemaining;
+	private float _reloadRemaining;
 
-    // Read-only surface for the HUD. Kept separate from the fields above so nothing outside
-    // this script can mutate combat state directly.
-    public string WeaponName => _currentWeapon?.WeaponName ?? string.Empty;
-    public int CurrentAmmo => _currentAmmo;
-    public int MagazineSize => _currentWeapon?.MagazineSize ?? 0;
-    public bool IsReloading => _reloadRemaining > 0.0f;
+	// Read-only surface for the HUD. Kept separate from the fields above so nothing outside
+	// this script can mutate combat state directly.
+	public string WeaponName => _currentWeapon?.WeaponName ?? string.Empty;
+	public int CurrentAmmo => _currentAmmo;
+	public int MagazineSize => _currentWeapon?.MagazineSize ?? 0;
+	public bool IsReloading => _reloadRemaining > 0.0f;
 
-    public override void _Ready()
-    {
-        _player = (CharacterBody3D)GetParent();
-        _attachment = GetNode<WeaponAttachment>("../Head/Armature_Arms/WeaponAttachment");
-        _camera = GetNode<Camera3D>("../Head/FirstPersonCamera");
-        _health = GetNodeOrNull<Health>("../Health");
-        _registry = ResourceLoader.Load<WeaponRegistry>("res://scripts/data/WeaponRegistry.tres")!;
-        Equip(0);
-    }
+	public override void _Ready()
+	{
+		_player = (CharacterBody3D)GetParent();
+		_attachment = GetNode<WeaponAttachment>("../Armature/WeaponAttachment");
+		_camera = GetNode<Camera3D>("../Head/FirstPersonCamera");
+		_health = GetNodeOrNull<Health>("../Health");
+		_registry = ResourceLoader.Load<WeaponRegistry>("res://scripts/data/WeaponRegistry.tres")!;
+		Equip(0);
+	}
 
-    public override void _Process(double delta)
-    {
-        if (!_player.IsMultiplayerAuthority()) return;
+	public override void _Process(double delta)
+	{
+		if (!_player.IsMultiplayerAuthority()) return;
 
-        for (int i = 0; i < Loadout.Length; i++)
-            if (Input.IsActionJustPressed($"weapon_{i + 1}")) Equip(i);
+		for (int i = 0; i < Loadout.Length; i++)
+			if (Input.IsActionJustPressed($"weapon_{i + 1}")) Equip(i);
 
-        if (_health != null && _health.IsDead) return;
+		if (_health != null && _health.IsDead) return;
 
-        float step = (float)delta;
-        _cooldownRemaining = Mathf.Max(0.0f, _cooldownRemaining - step);
-        if (_reloadRemaining > 0.0f)
-        {
-            _reloadRemaining -= step;
-            if (_reloadRemaining <= 0.0f) FinishReload();
-        }
+		float step = (float)delta;
+		_cooldownRemaining = Mathf.Max(0.0f, _cooldownRemaining - step);
+		if (_reloadRemaining > 0.0f)
+		{
+			_reloadRemaining -= step;
+			if (_reloadRemaining <= 0.0f) FinishReload();
+		}
 
-        if (Input.IsActionJustPressed("reload")) StartReload();
-        // Rate-limited by FireRateRPM below. Semi-auto weapons (Glock, Desert Eagle) should
-        // realistically require a fresh press per shot - not enforced yet, so holding fire
-        // will currently spam them at their listed cadence. Revisit alongside recoil.
-        if (Input.IsActionPressed("fire") && _cooldownRemaining <= 0.0f && _reloadRemaining <= 0.0f)
-            TryFire();
-    }
+		if (Input.IsActionJustPressed("reload")) StartReload();
+		// Rate-limited by FireRateRPM below. Semi-auto weapons (Glock, Desert Eagle) should
+		// realistically require a fresh press per shot - not enforced yet, so holding fire
+		// will currently spam them at their listed cadence. Revisit alongside recoil.
+		if (Input.IsActionPressed("fire") && _cooldownRemaining <= 0.0f && _reloadRemaining <= 0.0f)
+			TryFire();
+	}
 
-    private void Equip(int index)
-    {
-        foreach (var weapon in _registry.Weapons)
-        {
-            if (weapon.WeaponName != Loadout[index]) continue;
-            _currentWeapon = weapon;
-            _currentAmmo = weapon.MagazineSize;
-            _cooldownRemaining = 0.0f;
-            _reloadRemaining = 0.0f;
-            _attachment.Equip(weapon);
-            return;
-        }
-        GD.PushError($"Test loadout weapon '{Loadout[index]}' is not registered.");
-    }
+	private void Equip(int index)
+	{
+		foreach (var weapon in _registry.Weapons)
+		{
+			if (weapon.WeaponName != Loadout[index]) continue;
+			_currentWeapon = weapon;
+			_currentAmmo = weapon.MagazineSize;
+			_cooldownRemaining = 0.0f;
+			_reloadRemaining = 0.0f;
+			_attachment.Equip(weapon);
+			return;
+		}
+		GD.PushError($"Test loadout weapon '{Loadout[index]}' is not registered.");
+	}
 
-    private void TryFire()
-    {
-        if (_currentWeapon == null) return;
-        bool infiniteAmmo = _currentWeapon.MagazineSize <= 0;
-        if (!infiniteAmmo && _currentAmmo <= 0) { StartReload(); return; }
+	private void TryFire()
+	{
+		if (_currentWeapon == null) return;
+		bool infiniteAmmo = _currentWeapon.MagazineSize <= 0;
+		if (!infiniteAmmo && _currentAmmo <= 0) { StartReload(); return; }
 
-        _cooldownRemaining = _currentWeapon.FireRateRPM > 0.0f ? 60.0f / _currentWeapon.FireRateRPM : 0.5f;
-        if (!infiniteAmmo) _currentAmmo--;
-        _attachment.PlayFire();
-        FireHitscan();
-    }
+		_cooldownRemaining = _currentWeapon.FireRateRPM > 0.0f ? 60.0f / _currentWeapon.FireRateRPM : 0.5f;
+		if (!infiniteAmmo) _currentAmmo--;
+		_attachment.PlayFire();
+		FireHitscan();
+	}
 
-    private void FireHitscan()
-    {
-        bool melee = _currentWeapon!.WeaponClass == "Melee";
-        float range = melee ? MeleeRange : MaxRange;
-        Vector3 from = _camera.GlobalPosition;
-        Vector3 to = from + (-_camera.GlobalTransform.Basis.Z) * range;
+	private void FireHitscan()
+	{
+		bool melee = _currentWeapon!.WeaponClass == "Melee";
+		float range = melee ? MeleeRange : MaxRange;
+		Vector3 from = _camera.GlobalPosition;
+		Vector3 to = from + (-_camera.GlobalTransform.Basis.Z) * range;
 
-        var query = PhysicsRayQueryParameters3D.Create(from, to,
-            exclude: new Godot.Collections.Array<Rid> { _player.GetRid() });
-        var result = _player.GetWorld3D().DirectSpaceState.IntersectRay(query);
-        if (result.Count == 0) return;
+		var query = PhysicsRayQueryParameters3D.Create(from, to,
+			exclude: new Godot.Collections.Array<Rid> { _player.GetRid() });
+		var result = _player.GetWorld3D().DirectSpaceState.IntersectRay(query);
+		if (result.Count == 0) return;
 
-        // TODO: distinguish head vs. body hits once a dedicated head hitbox exists on the rig;
-        // every hit currently counts as BodyDamage, and HeadshotDamage goes unused.
-        if (result["collider"].As<Node>() is not CharacterBody3D body) return;
-        var targetHealth = body.GetNodeOrNull<Health>("Health");
-        if (targetHealth == null || targetHealth == _health) return;
+		// TODO: distinguish head vs. body hits once a dedicated head hitbox exists on the rig;
+		// every hit currently counts as BodyDamage, and HeadshotDamage goes unused.
+		if (result["collider"].As<Node>() is not CharacterBody3D body) return;
+		var targetHealth = body.GetNodeOrNull<Health>("Health");
+		if (targetHealth == null || targetHealth == _health) return;
 
-        int attackerId = _player.GetMultiplayerAuthority();
-        int targetId = targetHealth.GetMultiplayerAuthority();
-        targetHealth.RpcId(targetId, Health.MethodName.ReceiveDamage, _currentWeapon.BodyDamage, attackerId);
-    }
+		int attackerId = _player.GetMultiplayerAuthority();
+		int targetId = targetHealth.GetMultiplayerAuthority();
+		targetHealth.RpcId(targetId, Health.MethodName.ReceiveDamage, _currentWeapon.BodyDamage, attackerId);
+	}
 
-    private void StartReload()
-    {
-        if (_currentWeapon == null || _reloadRemaining > 0.0f) return;
-        bool infiniteAmmo = _currentWeapon.MagazineSize <= 0;
-        if (infiniteAmmo || _currentAmmo >= _currentWeapon.MagazineSize) return;
-        _reloadRemaining = _currentWeapon.ReloadTime > 0.0f ? _currentWeapon.ReloadTime : 0.1f;
-        _attachment.PlayReload();
-    }
+	private void StartReload()
+	{
+		if (_currentWeapon == null || _reloadRemaining > 0.0f) return;
+		bool infiniteAmmo = _currentWeapon.MagazineSize <= 0;
+		if (infiniteAmmo || _currentAmmo >= _currentWeapon.MagazineSize) return;
+		_reloadRemaining = _currentWeapon.ReloadTime > 0.0f ? _currentWeapon.ReloadTime : 0.1f;
+		_attachment.PlayReload();
+	}
 
-    private void FinishReload()
-    {
-        if (_currentWeapon != null) _currentAmmo = _currentWeapon.MagazineSize;
-        _reloadRemaining = 0.0f;
-    }
+	private void FinishReload()
+	{
+		if (_currentWeapon != null) _currentAmmo = _currentWeapon.MagazineSize;
+		_reloadRemaining = 0.0f;
+	}
 }
